# Name,   Type, SubType, Offset,    Size,     Flags
# Only used when TRENCHRUNNER is built and flashed on its own. Under MINIMAME the
# launcher's table is the one on the device, and this image is relocated into a
# game slot - IDF app images are position independent across app partitions.
nvs,      data, nvs,     0x9000,    0x6000,
phy_init, data, phy,     0xf000,    0x1000,
factory,  app,  factory, 0x10000,   0x100000,
